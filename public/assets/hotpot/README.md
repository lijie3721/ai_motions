# Hotpot demo media

These local demo images are used by the built-in hotpot media matcher so the
MVP can render restaurant and hotpot scenes without network access at render
time.

Sources: Wikimedia Commons file redirects downloaded into this workspace.
The app references only the local files under `public/assets/hotpot/`.
